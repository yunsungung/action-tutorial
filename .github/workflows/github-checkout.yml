name: github checkout

on: [push]

jobs:
  build:

    run-on: ubuntu-latest

    steps:
      - users: actions/checkout@v2
      - name: Run pwd
        run: pwd
      - name: Run ls -al
        run: ls -al
